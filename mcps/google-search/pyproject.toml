[project]
name = "google-search-mcp"
version = "0.1.0"
description = "Google Search via Gemini API (grounding nativo)"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
    "httpx>=0.27.0",
]

[project.scripts]
google-search-mcp = "google_search_mcp:main"
