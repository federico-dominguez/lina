# config/pipelines/healthcheck-hourly.yaml
# Se ejecuta cada hora por systemd timer
steps:
  - bot: lina
    msg: >
      Ejecutá el self-healthcheck: python3 ~/lina/bin/lina-self-health.py --fix --notify
      Mostrame el resultado.
